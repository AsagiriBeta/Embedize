# Embedize — whitelist isolation for structure datapacks
#
# Datapack structures generate ONLY in allowed-worlds.
# Vanilla minecraft: structures are never touched (any world).
# Manual /place is unaffected.

enabled: true

# Worlds that MAY receive managed structure datapacks (e.g. DnT).
allowed-worlds:
  - resource

# Which datapack structures Embedize manages:
#   ALL_NON_MINECRAFT — all non-vanilla namespaces (recommended)
#   NAMESPACES        — only the listed namespaces
structure-filter:
  mode: ALL_NON_MINECRAFT
  namespaces:
    - nova_structures

# If true, cancel when structure key cannot be resolved. Default false = leave alone.
deny-unresolved-structure-keys: false

debug-cancellations: false

datapacks:
  install-tfg-bridge: true

  dungeons-and-taverns:
    enabled: true
    install: auto
    modrinth-project: dungeons-and-taverns
    prefer-datapack-zip: true

  install-directory: default-world

multiverse:
  resolve-aliases: true
